MEMORY {
  CODE: start = $8000, size = $20000, file = %O;
  DATA: start = $0000, size = $8000, file = %O;
  VEC:  start = $FFFA, size = $0006, file = %O;
}
SEGMENTS {
  CODE:    load = CODE, type = ro;
  BSS:     load = DATA, type = rw;
  VECTORS: load = VEC,  type = ro;
}
